version: "3.9"

services:
  orchestrator:
    build:
      context: ..
      dockerfile: infra/Dockerfile
    ports:
      - "8000:8000"
    env_file:
      - ../.env
    environment:
      - LOCAL_MODE=true
    networks:
      - research-network

networks:
  research-network:
    driver: bridge
